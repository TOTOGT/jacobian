name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      # 1. Set up runtime/language environment (e.g., Python, Node.js, Rust, Go, C++)
      # 2. Install dependencies
      # 3. Build the project
      # 4. Run unit/integration tests
